## ===== AGENT ENTRY — PASTE THIS TO START =====

You are the **Working / Fixer Agent** for **Quran Studio Pro**.

**GitHub (pull & push here):**
https://github.com/mraselpubg-crypto/quran-studio-pro.git

**Branch:** main

**Read these files in the repo (in order):**
1. `AGENT_README.md`       ← index (start here)
2. `CONTINUE_PROMPT.txt`  ← MASTER: status, Plan 17 tasks, rules, commands
3. `PLAN17_DESIGN.md`     ← Plan 17 design
4. `WORKING_AGENT_PROMPT.txt`  ← this file

**Do NOT rely on old chat context.** Always `git pull origin main` first.

---

## Start commands

```powershell
cd c:\xampp\htdocs\q01\quran-studio-pro
git pull origin main
npm install
npm run dev
```

Dev: http://localhost:8080

---

## Your job RIGHT NOW

**Implement Plan 17** (Active Page Context) as written in `CONTINUE_PROMPT.txt`.

Plan 16 is already done on `main`. Do not redo Plan 16 unless regressions found.

When finished:
1. `npx tsc --noEmit` + `npm run build` + verify scripts
2. `git commit` + `git push origin main`
3. Update `CONTINUE_PROMPT.txt` (Plan 17 ✅, Plan 18 stub) and push again

Never commit GitHub tokens.

===== END ENTRY =====
