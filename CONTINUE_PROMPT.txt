## ===== WORKING AGENT HANDOFF — QURAN STUDIO PRO =====
## Agent Type: Testing + Bug Fixing + Next Plan Maker
## Date: 2026-05-23

---

## 🔗 GITHUB REPO

Remote: https://github.com/mraselpubg-crypto/quran-studio-pro.git
Branch: main
Local Path: c:\xampp\htdocs\q01\quran-studio-pro

## 📋 YOUR WORKFLOW

1. git pull origin main          ← always start here
2. npm install (if node_modules missing)
3. npm run dev (port 8080 — may already be running)
4. Inspect & fix any bugs found
5. npx tsc --noEmit              ← must exit 0 before commit
6. npm run build                 ← must exit 0 before commit
7. node scripts/verify-editor.mjs  ← Playwright visual/console check
8. git add -A && git commit && git push
9. Write the NEXT PLAN in CONTINUE_PROMPT.txt and push again

---

## 🏗️ PROJECT ARCHITECTURE

**Stack:** TanStack Start (React SSR) + Zustand + Fabric.js + Vite

**Key Source Files:**
- `src/components/studio/FabricLines.tsx`     — Canvas rows, key events (Enter/Delete/Type tool)
- `src/components/studio/PropertiesPanel.tsx` — Right-side properties + linking panel
- `src/components/studio/CanvasToolbar.tsx`   — Top toolbar (tools, scope, undo)
- `src/state/overridesStore.ts`               — Overrides + patchScoped + effectiveScope + resetScoped
- `src/state/reflowStore.ts`                  — Page layout, line distribution
- `src/state/editorStore.ts`                  — Active tool, scope, selection
- `src/state/linkingStore.ts`                 — Per-layer linking toggles (arabic/bangla/symbol)
- `src/state/historyStore.ts`                 — Undo/redo history + SCOPE_SUFFIX_BN labels
- `src/lib/textReflow.ts`                     — reflowFrom / backFillFrom cascade
- `scripts/verify-editor.mjs`                 — Playwright smoke test (screenshots in scripts/playwright-artifacts/)

---

## ✅ COMPLETED PLANS (Plans 1–14)

### Plan 14 ✅
- CharacterPanel: linking gate (patchLocal when OFF, patchScoped+effectiveScope when ON)
- SubLayerPanel: effectiveScope before patchScoped when linked
- resetScoped(scope, ctx) in overridesStore — scope-aware Reset All
- History labelBn includes scope suffix: (পেজ), (সূরা), (সকল)
- Playwright verify: no console errors on editor flow

---

## ⚠️ KNOWN ISSUES TO CHECK FIRST

1. **Reset without selection** — page/surah reset falls back to pages[0]; should use Workspace active page id
2. **DSlider vs CharacterPanel** — DSlider always uses effectiveScope; CharacterPanel uses patchLocal when unlinked (intentional; verify history entries match)
3. **Global reset** — still clears temporal + history stores entirely (by design in ResetGroup)

---

## 📝 NEXT PLAN TO IMPLEMENT (Plan 15)

### Feature: Active Page Context for Scope Operations

**Tasks:**

1. **Store active page id in editorStore**
   - Add `activePageId: string | null` + `setActivePageId`
   - Workspace.tsx calls setActivePageId when user changes page

2. **Use activePageId in ResetGroup and patchScoped fallbacks**
   - Reset page/surah scope uses activePageId instead of pages[0]

3. **History panel click-to-navigate**
   - Ensure scopeLabel + pageId jump highlights correct row in FabricLines

**Verification:**
- Reset with scope=page and no row selected clears only visible page overrides
- Playwright + manual: change page, reset page scope, confirm other pages untouched

**Commit message:** feat(Plan15): active page context for scoped reset and navigation

---

## 🔑 IMPORTANT RULES

1. NEVER use browser `confirm()` — use AlertDialog
2. ALL React hooks before early returns
3. Run tsc + build + verify-editor.mjs before commit
4. Use effectiveScope() for fan-out when linking is ON
5. NEVER fan out text content edits
6. Workflow order: **live → check → plan**

===== END OF HANDOFF PROMPT =====
