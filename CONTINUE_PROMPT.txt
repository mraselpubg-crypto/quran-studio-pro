## ===== WORKING AGENT HANDOFF — QURAN STUDIO PRO =====
## Date: 2026-05-23

---

## 🔗 REPO

Remote: https://github.com/mraselpubg-crypto/quran-studio-pro.git
Branch: main
Local: c:\xampp\htdocs\q01\quran-studio-pro
Dev: http://localhost:8080

Workflow: git pull → npm install → npm run dev → check → implement → tsc → build → verify → commit → push

---

## ✅ Plan 16 — Linked Area-Text Auto Reflow (COMPLETE)

- `effectiveReflowScope()` — `src/lib/reflowScope.ts`
- `reflowLayerText()` — `src/lib/textReflow.ts`
- `splitToFitForLayer()` — Arabic word-aware — `src/lib/canvasMeasure.ts`
- `patchTypographyScoped()` + guard — `src/lib/typographyReflow.ts`, `src/hooks/useTypographyPatch.ts`
- CharacterPanel + DSlider → typography reflow path
- LinkingPanel → "এরিয়া টেক্সট লিংক" copy
- Inline editor scope + link-off toast — `FabricLines.tsx`
- `scripts/verify-reflow.mjs` E2E smoke

Verify:
```powershell
npx tsc --noEmit
npm run build
node scripts/verify-editor.mjs
node scripts/verify-reflow.mjs
```

---

## 📝 Plan 17 — Active Page Context (NEXT)

1. `editorStore.activePageId` + Workspace sync
2. Reset page/surah uses active page (not `pages[0]`)
3. History click → navigate + row flash

Commit: `feat(Plan17): active page context for scoped reset and navigation`

---

## 🔑 RULES

1. `text` never patchScoped fan-out
2. reflowLayerText never calls rebuild()
3. Hooks before early returns
4. No confirm() — AlertDialog / CrossPageReflowDialog
5. Symbol layer: no text reflow

===== END =====
