## Working Agent Handoff — Plan 12 Verified ✅

### Completed (Plan 12) — feat(Plan12): cross-page reflow cascade, linking-aware scope gating, confirmation dialog

Browser verify (Editor mode → Type tool):
  ✓ Enter mid-line pushes trailing text to next row (same page → no dialog)
  ✓ When overflow lands on next page → "লেআউট পরিবর্তন নিশ্চিত করুন?" dialog appears
  ✓ "বাতিল" restores the original line text; "হ্যাঁ" applies the cascade
  ✓ When overflow crosses a Surah boundary → dialog mentions "ও সূরায়"
  ✓ Linking switch OFF for arabic + scope=surah → font/Y slider only affects current row
  ✓ Linking switch ON for arabic + scope=surah → font/Y propagates across surah
  ✓ Same applies for bangla and symbol layers independently
  ✓ npx tsc --noEmit exits 0
  ✓ npm run build exits 0

Commit: feat(Plan12): cross-page reflow cascade, linking-aware scope gating, confirmation dialog

---

## Next Plan — Plan 13 (Pending User Approval)

### Feature: Dynamic Paragraph Linking — Scope-Aware Tool Integration

**Goal:** Make the paragraph linking system fully functional and context-aware based on active tool and selected layer.

#### Requirements:

1. **Scope-Aware Linking Indicator:**
   - When a specific layer (Arabic / Bangla / Symbol) is selected AND the Linking switch is ON for that layer:
     - Highlight/indicate visually in the Properties panel which scope (General / Page / Surah / Global) is currently active.
     - The linking switch state should be visually reflected per-layer with a badge or glow effect.

2. **Tool-Aware Linking Activation:**
   - Selection Tool (Move / "V"): Linking switch affects position (dx, dy) propagation.
   - Type Tool ("T"): Linking switch affects font/Y slider propagation.
   - Both tools should obey the same per-layer ON/OFF state from `useLinkingStore`.

3. **Cross-Layer Interaction Protection:**
   - If Arabic linking is ON and Bangla linking is OFF:
     - Arabic font/Y changes → propagate per scope
     - Bangla font/Y changes → only affect the current selected row (no fan-out)
   - Symbol layer should behave independently.

4. **`effectiveScope()` function validation:**
   - Verify that `effectiveScope()` in `PropertiesPanel.tsx` correctly reads the active layer from `selection.layerKind`.
   - If `layerKind` is null/undefined (row-level selection), fall back to the user's current scope setting.
   - Add a unit/manual test: select a row → switch to Arabic layer → turn linking ON → drag font slider → confirm all arabic rows in the surah update.

5. **Visual Feedback in LinkingPanel:**
   - Show the currently active scope next to each layer toggle in `LinkingPanel`.
   - Example: "আরবি লিংকিং [সূরা]" with scope label.

#### Files to modify:
- `src/components/studio/PropertiesPanel.tsx` — `LinkingPanel` and `DSlider`
- `src/state/linkingStore.ts` — verify layer-kind state shape
- `src/state/editorStore.ts` — verify `selection.layerKind` is correctly set when clicking each sub-layer

#### Verification:
- npx tsc --noEmit exits 0
- npm run build exits 0
- Browser test: all 7 linking scenarios pass
- Commit: feat(Plan13): scope-aware linking UI, tool-aware propagation, layer protection

---

### GitHub Remote (for push):
Remote URL: https://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@github.com/mraselpubg-crypto/quran-studio-pro.git
Branch: main

### After completing Plan 13:
1. Run: npx tsc --noEmit
2. Run: npm run build
3. Commit with: git commit -am "feat(Plan13): ..."
4. Push: git push origin main
