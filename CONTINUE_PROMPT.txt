## ===== MASTER AGENT HANDOFF — QURAN STUDIO PRO =====
## Single source of truth — read this first, update this when done, push to GitHub
## Last updated: 2026-05-23

---

## 🧠 AGENT MEMORY RULE (সব এজেন্ট — অবশ্যই)

**একটি Plan শেষ = কোড + verify + push + হ্যান্ডঅ ফাইল আপডেট + আবার push.**

চ্যাটে “done” বললেই হবে না। পরবর্তী এজেন্ট **শুধু এই রিপোর ফাইল** পড়ে শুরু করবে।  
বিস্তারিত: **`PLAN_COMPLETION_PROTOCOL.md`** — প্রতিটি Plan শেষে সেটা অনুসরণ করো।

| Plan শেষে আপডেট করো |
|---------------------|
| `CONTINUE_PROMPT.txt` — ✅ mark + পরের Plan লিখো |
| `AGENT_README.md` — স্ট্যাটাস টেবিল |
| `WORKING_AGENT_PROMPT.txt` — পরের Plan নম্বর |
| প্রয়োজনে `PLAN(N+1)_DESIGN.md` + `PLAN(N+1)_AGENT_PROMPT.txt` |

---

## 🔗 REPOSITORY (ONLY OFFICIAL REMOTE)

| Item | Value |
|------|--------|
| **GitHub** | https://github.com/mraselpubg-crypto/quran-studio-pro.git |
| **Branch** | `main` |
| **Local (Windows)** | `c:\xampp\htdocs\q01\quran-studio-pro` |
| **Dev server** | http://localhost:8080 |
| **Start dev** | `npm run dev` |

**Security:** Never paste GitHub PAT/tokens in chat or commit them. Use local `git credential` for push.

---

## 📋 MANDATORY WORKFLOW (every session)

```
1. git pull origin main
2. npm install          (if node_modules missing)
3. npm run dev          (port 8080; kill old process if "already in use")
4. Read this file + WORKING_AGENT_PROMPT.txt
5. Fix bugs if any → implement CURRENT PLAN below
6. npx tsc --noEmit     (must exit 0)
7. npm run build        (must exit 0)
8. node scripts/verify-editor.mjs
9. node scripts/verify-reflow.mjs   (after Plan 16+)
10. git add -A
11. git commit -m "feat(PlanXX): …" or "fix(…): …"
12. git push origin main
13. PLAN COMPLETION PROTOCOL (PLAN_COMPLETION_PROTOCOL.md):
    - Mark current plan ✅ in THIS FILE
    - Write NEXT plan tasks in THIS FILE
    - Update AGENT_README.md + WORKING_AGENT_PROMPT.txt
14. git commit -m "docs: handoff after PlanXX — PlanYY ready"
15. git push origin main
```

Order: **live → check → code → verify → commit → handoff files → commit → push**

---

## 📁 HANDOFF FILES IN THIS REPO (read order)

| File | Purpose |
|------|---------|
| **AGENT_README.md** | Entry point — GitHub Extension + copy-paste prompt |
| **WORKING_AGENT_PROMPT.txt** | Short agent entry |
| **CONTINUE_PROMPT.txt** | Master status, plans, rules (THIS FILE) |
| **PLAN17_DESIGN.md** | Plan 17 design (done) |
| **PLAN18_DESIGN.md** | Plan 18 design doc |
| **PLAN_COMPLETION_PROTOCOL.md** | Plan শেষে ফাইল আপডেটের নিয়ম |
| **AGENT_PROMPT.md** | Legacy v2 — ignore |

---

## 🏗️ STACK & KEY PATHS

**Stack:** TanStack Start + React + Zustand + Fabric/HTML rows + Vite

| Area | Path |
|------|------|
| Canvas rows / Type tool / reflow UI | `src/components/studio/FabricLines.tsx` |
| Properties + Linking + History | `src/components/studio/PropertiesPanel.tsx` |
| Workspace / active page UI | `src/components/studio/Workspace.tsx` |
| Overrides + patchScoped + resetScoped | `src/state/overridesStore.ts` |
| Editor scope, selection, navigateTo | `src/state/editorStore.ts` |
| Page list / distribution | `src/state/reflowStore.ts` |
| Paragraph linking toggles | `src/state/linkingStore.ts` |
| History labels | `src/state/historyStore.ts` |
| Reflow engine | `src/lib/textReflow.ts` |
| Reflow scope (link + editor scope) | `src/lib/reflowScope.ts` |
| Typography → reflow | `src/lib/typographyReflow.ts` |
| Typography hook + guard | `src/hooks/useTypographyPatch.ts` |
| Playwright smoke | `scripts/verify-editor.mjs`, `scripts/verify-reflow.mjs` |

**Artboard text width for reflow:** `780 - 16` px (`ARTBOARD_TEXT_WIDTH` in typographyReflow.ts)

---

## ✅ COMPLETED PLANS

### Plan 14 ✅ — Scope reset, linking gate, history labels
- `resetScoped()`, CharacterPanel linking, SubLayerPanel `effectiveScope`
- History `labelBn` suffix: (পেজ), (সূরা), (সকল)

### Plan 16 ✅ — Linked Area-Text Auto Reflow (commit ~6256a1f)
- `effectiveReflowScope()` — `src/lib/reflowScope.ts`
- `reflowLayerText()` — `src/lib/textReflow.ts`
- `splitToFitForLayer()` — Arabic word-aware — `src/lib/canvasMeasure.ts`
- `patchTypographyScoped()` + `useTypographyPatch` + large-change guard
- CharacterPanel + DSlider → typography fields trigger reflow
- LinkingPanel → "এরিয়া টেক্সট লিংক"
- Inline editor: scope + link-off toast / clip
- `scripts/verify-reflow.mjs`

### Plan 17 ✅ — Active Page Context (local implement 2026-05-23)
- `editorStore`: `activePageId`, `setActivePageId`; `navigateTo` sets active page + row flash ~1.2s
- `Workspace`: store-driven visible page; legacy `navigateToPageId` cleared without killing flash
- `ResetGroup`: `getContextPageId()` — selection → active page → first page
- `HistoryTab`: click entry → navigate; restore uses `stopPropagation`
- `src/lib/editorContext.ts` — `getContextPageId()`
- verify: tsc, build, verify-editor.mjs, verify-reflow.mjs

---

### Plan 18 ✅ — Context Page Unification + verify-active-page (commit ~665bcfc)
- `getContextPageId()` unified fallback across `reflowScope` and `typographyReflow`.
- Added Playwright script `scripts/verify-active-page.mjs` verifying context page scoped reset.
- Fixed critical infinite rendering loop in `CanvasToolbar` and `PropertiesPanel` by wrapping Zustand `sessionEntries()` selectors in `useShallow`.

---

## ✅ Plan 19 — Complete All-114-Surah Sidebar (still active)

> Keep Plan 19 as the current implementation target until it is fully done.

## 🎯 CURRENT PLAN — Plan 19: Complete All-114-Surah Sidebar (IMPLEMENT NEXT)

### Problem (Audit Finding — 2026-05-23)
`PageList.tsx` already has collapsible Surah grouping + search UI. But:
- `SURAH_NAMES` map has only **9 of 114** Bengali names → remaining 105 show `"সূরা N"`
- `SURAH_NAMES_AR` map has only **9 of 114** Arabic names
- No English transliteration map → English search is broken
- Surah header click only toggles expand — does NOT navigate canvas to that Surah's first page
- No verse count shown in header

### Tasks

**1. Complete `SURAH_NAMES` — all 114 Bengali transliterated names**
- File: `src/components/studio/PageList.tsx`
- Fill the full `Record<number, string>` for surahs 1–114

**2. Complete `SURAH_NAMES_AR` — all 114 Arabic names**
- File: `src/components/studio/PageList.tsx`
- Fill the full `Record<number, string>` for surahs 1–114

**3. Add `SURAH_NAMES_EN` — English transliteration map (1–114)**
- File: `src/components/studio/PageList.tsx`
- Update search filter to also match English names (e.g. "Al-Baqarah")

**4. Surah header click → navigate canvas to first page**
- File: `src/components/studio/PageList.tsx`
- On header click: call `onSelect(pages[0].pageId)` after toggle

**5. Add ayah count to Surah header**
- Add `SURAH_AYAH_COUNT: Record<number, number>` (114 entries)
- Display e.g. `(٧ آيات)` or `(৭ আয়াত)` in header

**6. New Playwright verification script**
- Create `scripts/verify-sidebar.mjs`
- Test: type "আল-বাকারা" → only Surah 2 visible → click header → canvas navigates

### Files to modify
| File | Change |
|------|--------|
| `src/components/studio/PageList.tsx` | Add all 114 names + EN map + navigation on click + ayah count |
| `scripts/verify-sidebar.mjs` | **NEW** — Playwright sidebar smoke |

### Verification
```powershell
npx tsc --noEmit
npm run build
node scripts/verify-editor.mjs
node scripts/verify-reflow.mjs
node scripts/verify-active-page.mjs
node scripts/verify-sidebar.mjs
```

### Commit message
```
feat(Plan19): complete all-114-Surah sidebar with Bengali/Arabic/English names, search, and navigation
```

---

## ⚠️ KNOWN ISSUES / WATCH

1. **verify-reflow** `link-off-toast` case flaky in Playwright (Sonner portal) — optional in CI
2. **Global font slider** (`setGlobal`) does not run typography reflow — by design (row-local only)
3. **routeTree.gen.ts** — auto-generated; do not commit unless intentionally changed
4. **Port 8080** — if `EADDRINUSE`, stop other Vite or use different port

---

## 🔑 INVARIANTS (never break)

| # | Rule |
|---|------|
| 1 | `text` field never `patchScoped` fan-out — per-row `reflowLayerText` only |
| 2 | `reflowLayerText()` never calls `reflowStore.rebuild()` |
| 3 | All React hooks before any early `return` |
| 4 | No `confirm()` — use `AlertDialog` / `CrossPageReflowDialog` |
| 5 | Symbol layer: no text reflow |
| 6 | Use `effectiveScope()` for property fan-out when linking ON |
| 7 | Use `effectiveReflowScope()` for text cascade boundaries |

---

## 🔧 QUICK COMMANDS (PowerShell)

```powershell
cd c:\xampp\htdocs\q01\quran-studio-pro
git pull origin main
npm install
npm run dev
npx tsc --noEmit
npm run build
node scripts/verify-editor.mjs
node scripts/verify-reflow.mjs
git add -A
git commit -m "feat(Plan18): unify context page id and add active-page verify script"
git push origin main
```

---

===== END MASTER HANDOFF — update after each plan =====
